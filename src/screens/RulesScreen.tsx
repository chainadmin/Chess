import React from 'react';
import { ScrollView, Text, StyleSheet } from 'react-native';
import { Screen } from '../components/Screen';
export function RulesScreen() { return <Screen><ScrollView><Text style={styles.title}>Rules</Text><Text style={styles.copy}>ShiftChess is played on a 10 columns × 8 rows board. Each back rank starts: R N S B Q K B S N R, with 10 pawns per side.</Text><Text style={styles.heading}>Shifter (S)</Text><Text style={styles.copy}>The Shifter may move like a bishop any number of squares diagonally. It cannot jump and may capture on diagonal moves.</Text><Text style={styles.copy}>Instead, it may shift exactly one square orthogonally: up, down, left, or right. Captures are allowed on this one-square shift.</Text><Text style={styles.copy}>The Shifter is not a queen and cannot move multiple orthogonal squares. It is not a king; diagonal movement is legal only as a bishop path.</Text></ScrollView></Screen>; }
const styles = StyleSheet.create({ title:{fontSize:34,fontWeight:'900',color:'#f7ead2',marginBottom:16}, heading:{fontSize:24,fontWeight:'900',color:'#75fbff',marginTop:18}, copy:{color:'#e9d8c0',fontSize:16,lineHeight:24,marginVertical:8} });
